# 音频卡顿分析工具打包说明

## 环境准备

### 1. 安装Python依赖
```bash
pip install -r requirements.txt
```

### 2. 打包成可执行文件
```bash
pyinstaller -D frame_to_wav_run.spec
```

或者使用命令行方式（如果spec文件有问题）：
```bash
pyinstaller -D -w -i wav.ico --add-data "get_data;get_data" --add-data "gui;gui" --add-data "services;services" --add-data "utils;utils" --add-data "analysis;analysis" --add-data "business;business" --add-data "pakcet;pakcet" frame_to_wav_run.py
```

## 打包文件说明

### 生成的可执行文件
- 位于 `dist/frame_to_wav_run/` 目录下
- 包含所有必要的依赖库和资源文件

### 打包内容
- 主程序: frame_to_wav_run.exe
- 配置文件: get_data/base/myconfig.yml
- 图标文件: wav.ico
- 所有Python模块和依赖库

## 注意事项

1. 打包前确保已安装所有依赖项
2. 打包过程可能需要几分钟时间，请耐心等待
3. 如果出现错误，请检查requirements.txt中的依赖版本是否兼容
4. 打包完成后，可以在没有安装Python环境的电脑上运行

## 故障排除

如果打包失败，请尝试：
1. 升级PyInstaller: `pip install --upgrade pyinstaller`
2. 清理缓存: 删除 `build/` 和 `dist/` 目录后重新打包
3. 检查是否有权限问题